import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.tablaDidactica.lcea',
  appName: 'tabla-didactica',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
